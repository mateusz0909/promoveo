{
  "name": "lemmi-studio",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client & npm run dev --workspace=server",
    "start": "npm start --workspace=server"
  },
  "dependencies": {
    "archiver": "^7.0.1"
  },
  "devDependencies": {
    "supabase": "^2.40.7"
  }
}
